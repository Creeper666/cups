<H2 CLASS="title">设 {is_class?类:打印机} {printer_name} 为默认</H2>

<P>{is_class?类:打印机} <A
HREF="/{is_class?classes:printers}/{printer_name}">{printer_name}</A>
已被设置为本服务器的默认打印机。</P>

<BLOCKQUOTE><B>注意：</B>通过<TT>lpoptions</TT>命令设置的任何用户默认值都将覆盖此默认设置。</BLOCKQUOTE>
